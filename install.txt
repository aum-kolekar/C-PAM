===============Install auditd==================
sudo apt install auditd audispd-plugins -y
sudo systemctl enable auditd
sudo systemctl start auditd
sudo auditctl -w /usr/bin/sudo -p x -k sudo_monitor     //monitors sudo usages
sudo auditctl -w /etc/passwd -p wa -k passwd_changes    //monitors file delitions

==============install filebeat=================
wget -qO - https://artifacts.elastic.co/GPG-KEY-elasticsearch | sudo apt-key add -
echo "deb https://artifacts.elastic.co/packages/8.x/apt stable main" | sudo tee /etc/apt/sources.list.d/elastic-8.x.list
sudo apt update
sudo apt install filebeat -y
sudo apt install filebeat -y

--------------------------
change config file @ /etc/filebeat/filebeat.yml

=============install elasticsearch==============
sudo apt update
sudo apt install wget gnupg apt-transport-https -y
wget -qO - https://artifacts.elastic.co/GPG-KEY-elasticsearch | sudo gpg --dearmor -o /usr/share/keyrings/elastic-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/elastic-keyring.gpg] https://artifacts.elastic.co/packages/8.x/apt stable main" | sudo tee /etc/apt/sources.list.d/elastic-8.x.list
sudo apt update
sudo apt install elasticsearch -y
sudo systemctl daemon-reexec
sudo systemctl enable elasticsearch
sudo systemctl start elasticsearch

    **test elasticsearch**
    curl -k http://localhost:9200

    **get elasticsearch password**
    sudo /usr/share/elasticsearch/bin/elasticsearch-reset-password -u elastic

===========install logstash=====================
sudo apt update
sudo apt install logstash -y

----------------------------------
##verify
sudo journalctl -u logstash -f
curl -k -u elastic https://localhost:9200/_cat/indices?v 
----------------------------------
change config @ /etc/logstash/conf.d/filebeat.conf


==============install kibana=======================
sudo apt update
sudo apt install kibana -y
--------------
change kibana.yml @ /etc/kibana/kibana.yml
