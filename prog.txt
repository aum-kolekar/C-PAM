elasticsearch security key 
elastic : bSXq*R0G*7vTwyBhKRWZ
kibana_system : N+7x7tdcPq9jiCl3cZHH

check the data is coming from client1:
====command====
curl -k -u elastic https://localhost:9200/filebeat-*/_search \          
'Content-Type: application/json' -d '
{
  "query": {
    "match": {
      "host.name": "client1"
    }
  }
}'


ToDo : 
  -setupproper fields for log.
  -setup logstash (data cleaner)

prog:
  -setup elasticsearch, audit, filebeat on kali.
  -install vm and vagrant.
  -setup 1 vm -(client1) setup 
  -verfy logs coming from client1 vm to elasticsearch
    - status: "All works"
  -setting up kibana for data visualization.
    -status: "FAILED" //signing off.
  -filebeat on vm file, ships logs to elastic _search
  -kibana setup done, logs visible on the localhost:5601
  -linux currupted.
  -lost entire progress.
  -deleted linux.
  -windows bootloader failed.
  -shift to windows.
  -install linux on virtual box.
  -audit setup complete
  -filebeat setup complete
  -elasticsearch setup complete
  -kibana: unable to connect.
  -kibana setup complete.
